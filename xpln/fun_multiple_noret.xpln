a := 2;
b := 8;

fun f(x, y)
    r := 1;
    while y > 0
        r := r * x;
        y := y - 1;
    endw;
    return r;
endf;

fun g(x, y)
    r := 1;
    while y > 0
        r := r * x;
        y := y - 1;
    endw;
endf;

c := 3 + f(a + 1, b / 4) * (4 + 2);
output c;
return c;
